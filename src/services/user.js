/**
 * @description user services
 * @author cyq
 */
const { User } = require('../db/module/index');

/**
 * 
 * @param {*} yserName 用户名
 * @param {*} passWord 密码
 */
async function getUserInfo(yserName, passWord) {

}

module.exports = {
  getUserInfo
}